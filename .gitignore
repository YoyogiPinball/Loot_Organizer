# ログファイル
logs/
*.log

# Python関連
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/

# OS関連
.DS_Store
Thumbs.db
desktop.ini
*Zone.Identifier*

# ユーザーの実行モードファイル（mode 直下のみ除外）
# `*` は `/` に一致しないので mode/samples/ 配下は元から対象外
mode/*.yaml

# 開発用ドキュメント（GitHubには含めない）
CLAUDE.md
devnote.md
devnote.pdf
docs_ToMe/
_Archives/
*.lnk

# 個人用プリセット（実際の保存先パスを含むため公開しない）
configs_myrule/

# ローカルのエディタ・エージェント設定
.claude/

# カラープレビュースクリプト（開発用）
preview_cyberpunk.py

# 移行元スクリプト（個人的な設定含む）
MigrationSource/

# テストデータ
test_data/

# IDE関連
.vscode/
.idea/
*.swp
*.swo
*~

# 仮想環境
venv/
env/
ENV/
.venv/

# 各自の好きなツールを使う方針のため、フック設定はリポジトリに含めない
lefthook.yml

# 本番プリセット（mode/ 直下・git 管理外）を読む個人用の結合テスト。
# 実在するフォルダ構成を含み、クローン先では実行できないためリポジトリに含めない。
tests/test_my_pipeline_integration.py
